<% if object.embed? %>
  <div class="modal invisible fade embed" id="<%= object.title.parameterize %>_embed">
    <%= markdown_embed(object.embed) %>
    <%= link_to t('.close'), '#', 'data-dismiss' => 'modal', 'class' => 'btn btn-primary' %>
  </div>
<% end %>
